public enum EDirection
{
    None,
    North, //z轴正方向
    East, //x轴正方向
    South,
    West,
}

public enum ERoomType
{
    None,
    NormalRoom,
    StartRoom,
    EndRoom,
    // BossRoom,
    // ShopRoom,
    // TreasureRoom,
    // SecretRoom,
}